import 'package:flutter/material.dart';

class AnalisiView extends StatelessWidget {
  const AnalisiView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("schermata di analisi"));
  }
}
