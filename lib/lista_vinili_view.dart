import 'package:flutter/material.dart';

class ListaViniliView extends StatelessWidget {
  const ListaViniliView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("schermata di lista vinili"));
  }
}